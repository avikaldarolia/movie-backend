const NUMBER = 'number';
const FLOAT = 'float';
const STRING = 'string';
const ARRAY = 'array';
const DATE = 'date';
const BOOLEAN = 'boolean';
const TEXT = 'text';
const INTEGER = 'integer';
const OBJECT = 'object';
const JSON = 'object';

const ALLOWED_DATA_TYPES = [NUMBER, FLOAT, STRING, ARRAY, DATE, BOOLEAN, TEXT, INTEGER, OBJECT, JSON];

module.exports = {
    NUMBER,
    FLOAT,
    STRING,
    ARRAY,
    DATE,
    BOOLEAN,
    TEXT,
    INTEGER,
    OBJECT,
    JSON,
    ALLOWED_DATA_TYPES
};